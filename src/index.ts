


let numInput:number = Number(prompt("ingrese numero"));
let mayor = numInput;
let menor = numInput;

while (numInput != 0){
  numInput = Number(prompt("ingrese numero"));
  if (mayor = numInput)
  console.log ("el numero mayor es:", numInput);
  else (menor = numInput)
  console.log ("el numero menor es:", numInput);
}
}